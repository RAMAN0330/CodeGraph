import LegacyWorkspaceEngine from '../legacy/LegacyWorkspaceEngine';

/** Typed route boundary for the workspace feature. */
export default function WorkspaceArea() {
  return <LegacyWorkspaceEngine />;
}
